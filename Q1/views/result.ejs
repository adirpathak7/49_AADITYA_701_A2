<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Registration Result</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 min-h-screen flex flex-col items-center justify-center px-4 py-8">

  <h2 class="text-3xl font-bold text-green-600 mb-6">Registration Successful</h2>

  <div class="bg-white shadow-md rounded-lg overflow-hidden w-full max-w-3xl">
    <table class="min-w-full text-left text-sm text-gray-700">
      <tbody>
        <tr class="border-b">
          <td class="px-6 py-4 font-medium">Username</td>
          <td class="px-6 py-4">
            <%= user.username %>
          </td>
        </tr>
        <tr class="border-b">
          <td class="px-6 py-4 font-medium">Email</td>
          <td class="px-6 py-4">
            <%= user.email %>
          </td>
        </tr>
        <tr class="border-b">
          <td class="px-6 py-4 font-medium">Gender</td>
          <td class="px-6 py-4">
            <%= user.gender %>
          </td>
        </tr>
        <tr class="border-b">
          <td class="px-6 py-4 font-medium">Hobbies</td>
          <td class="px-6 py-4">
            <%= user.hobbies.join(", ") %></td>
        </tr>
        <tr class=" border-b">
          <td class="px-6 py-4 font-medium">Profile Pic</td>
          <td class="px-6 py-4">
            <img src="/uploads/<%= user.profilePic %>" alt="Profile Picture"
              class="w-24 h-24 object-cover rounded-md border" />
          </td>
        </tr>
        <tr>
          <td class="px-6 py-4 font-medium">Other Pics</td>
          <td class="px-6 py-4 flex gap-3 flex-wrap">
            <% user.otherPics.forEach(pic=> { %>
              <img src="/uploads/<%= pic %>" alt="Other Pic" class="w-20 h-20 object-cover rounded border" />
              <% }) %>
          </td>
        </tr>
      </tbody>
    </table>
  </div>

  <a href="/download/<%= file %>"
    class="mt-6 inline-block bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700 transition">Download your Data</a>

</body>

</html>